export interface blockDataType {
  name: string;
  age: number;
}
export interface tableConfigType {
  label: string;
  name: string;
  field: string;
  align: string;
}
export interface tableButtonType {
  label: string;
  icon: string;
  status: string;
}
